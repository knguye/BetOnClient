import React from 'react';
import renderer from 'react-test-renderer';
import { RegistrationScreen } from '../src/screens';

test('Registration with existing user does not upload data to firebase & backend', () => {
    
})